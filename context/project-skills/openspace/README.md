## OpenSpace Project Skills

This directory mirrors project-scoped skills that are intentionally installed
outside the repository under `~/.codex/projects/openspace/skills/`.

Use it to keep reusable project-routing and workflow skills versioned in Git so
they can be reviewed, migrated to other machines, and reinstalled into the
project bucket when needed.
